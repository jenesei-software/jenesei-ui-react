import { Typography } from '@components/typography'

import { PreviewWrapper } from '.'

export const Preview = () => {
  return (
    <PreviewWrapper>
      <Typography weight={800} size={24}>
        Jenesei Software
      </Typography>
    </PreviewWrapper>
  )
}
