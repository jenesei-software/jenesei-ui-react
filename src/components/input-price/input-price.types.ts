import { InputDefaultProps } from '../../main'
import { CurrencyInputProps } from 'react-currency-input-field'

export interface InputPriceProps extends InputDefaultProps {
  libraryProps?: CurrencyInputProps
}
